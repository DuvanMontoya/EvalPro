# Archivo de analisis intencionalmente minimo para evitar reglas no requeridas por el proyecto.
analyzer:
  exclude:
    - "**/*.g.dart"
    - "packages/flutter_windowmanager/**"
    - "packages/screenshot_callback/**"
